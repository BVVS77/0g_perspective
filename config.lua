Config = {}

-- Toggle key (default F6)
Config.ToggleKey = 167

-- Framework notifications ox / esx / chat / qb
Config.Framework = "chat"

-- Default view
Config.DefaultView = 0

-- Allowed views
Config.AllowedViews = {
    [4] = true,
    [0] = true,
    [2] = true
}

-- Locale
Config.Locale = "en"

-- Admin permissions:
-- "ace" = use ACE permissions (recommended)
-- "none" = no check (everyone can use, not safe!)
Config.AdminSystem = "ace"

-- add_ace group.admin zerogravity.forceview allow <